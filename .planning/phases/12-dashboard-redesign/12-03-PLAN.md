---
phase: 12-dashboard-redesign
plan: 03
type: execute
---

<objective>
Redesign agent creation/edit forms, settings page forms, and credit pack cards with shadcn/ui components.

Purpose: Apply consistent premium styling to all form elements and purchase cards.
Output: Polished forms using shadcn/ui Input, Label, Button with glassmorphic containers.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-dashboard-redesign/12-CONTEXT.md
@.planning/phases/12-dashboard-redesign/12-01-SUMMARY.md
@.planning/phases/12-dashboard-redesign/12-02-SUMMARY.md

**Key files to redesign:**
- src/app/(dashboard)/dashboard/agents/new/page.tsx (agent creation form)
- src/app/(dashboard)/dashboard/agents/[id]/edit/page.tsx (agent edit form)
- src/app/(dashboard)/dashboard/settings/page.tsx (settings page)
- src/components/dashboard/credit-pack-card.tsx (credit purchase card)

**shadcn/ui components available:**
- Button, Card, Input, Label (from Phase 11)
- Dialog, DropdownMenu, Separator (from Phase 11)

**Form patterns to apply:**
- Label above Input with proper spacing
- Input with focus:ring-primary styling
- Button variants: default (primary), outline (secondary), destructive
- Form sections wrapped in glass-card
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Textarea and Switch components, redesign agent forms</name>
  <files>src/components/ui/textarea.tsx, src/components/ui/switch.tsx, src/app/(dashboard)/dashboard/agents/new/page.tsx, src/app/(dashboard)/dashboard/agents/[id]/edit/page.tsx</files>
  <action>
**First, add missing components:**
Run: npx shadcn@latest add textarea switch

**Then redesign agent creation form (agents/new/page.tsx):**
1. Read the current file structure first
2. Wrap form in Card with glass-card class
3. Use CardHeader for page title
4. Use CardContent for form fields
5. Replace all input elements with shadcn/ui Input
6. Replace all textareas with shadcn/ui Textarea
7. Use Label component above each field
8. Group related fields in sections with Separator between
9. Submit button: Button variant="default" with gradient hover effect
10. Cancel button: Button variant="outline"

**Redesign agent edit form (agents/[id]/edit/page.tsx):**
1. Apply same patterns as creation form
2. Add Switch component for isActive toggle (if applicable)
3. Use consistent spacing: space-y-6 for sections, space-y-2 for label+input

**Form field styling:**
- Label: text-sm font-medium text-foreground
- Input/Textarea: Use component defaults (already styled for dark mode)
- Add focus-visible:ring-primary to inputs if not default
- Error states: text-destructive for error messages
  </action>
  <verify>npm run dev - navigate to /dashboard/agents/new, form displays with shadcn/ui components, submit works</verify>
  <done>Agent forms use shadcn/ui Input, Textarea, Label, Button, Switch - all fields styled consistently</done>
</task>

<task type="auto">
  <name>Task 2: Redesign settings page with shadcn/ui components</name>
  <files>src/app/(dashboard)/dashboard/settings/page.tsx, src/components/dashboard/transaction-list.tsx</files>
  <action>
**Redesign settings page:**
1. Read the current settings page structure first
2. Section cards: Use Card with glass-card class
3. Section titles: CardHeader with CardTitle
4. Content: CardContent with proper spacing

**Account info section:**
- Display fields in a clean grid or stack layout
- Use text-muted-foreground for labels, text-foreground for values
- If editable: Use Input components with Label

**Transaction history section:**
- Table or list with glass-card container
- Use Separator between rows
- Status badges: Use Badge component
- Amount: Use text-primary for positive, text-destructive for negative
- Date formatting: text-muted-foreground text-sm

**transaction-list.tsx (if exists):**
1. Read current implementation
2. Apply Card wrapper with glass-card
3. Style table rows with hover:bg-muted/50
4. Use Badge for transaction types
5. Consistent typography with muted-foreground for secondary text

**Empty states:**
- Center content with icon
- text-muted-foreground for message
- Subtle border or background
  </action>
  <verify>npm run dev - navigate to /dashboard/settings, all sections styled, transaction history displays</verify>
  <done>Settings page uses Card components, transaction list styled with badges and proper typography</done>
</task>

<task type="auto">
  <name>Task 3: Redesign credit-pack-card with premium styling</name>
  <files>src/components/dashboard/credit-pack-card.tsx, src/app/(dashboard)/dashboard/credits/page.tsx</files>
  <action>
**Redesign credit-pack-card.tsx:**
1. Read current implementation first
2. Use Card component with glass-card class
3. Popular pack: Add gradient border effect
   - Use relative positioning with pseudo-element for gradient border
   - Or use ring with ring-primary styling
4. Popular badge: Use Badge with bg-primary text-primary-foreground
5. Price display:
   - Large price: text-4xl font-bold text-foreground
   - Per-minute rate: text-muted-foreground
6. Credits amount: Highlight with text-primary
7. Purchase button:
   - Regular packs: Button variant="outline"
   - Popular pack: Button variant="default" with gradient background
8. Hover effect: Increase glow intensity, subtle scale transform

**Structure:**
```
<Card className={cn("glass-card", isPopular && "ring-2 ring-primary")}>
  {isPopular && <Badge className="absolute -top-3 ...">Most Popular</Badge>}
  <CardHeader>
    <CardTitle>{name}</CardTitle>
  </CardHeader>
  <CardContent>
    <div className="text-4xl font-bold">${price}</div>
    <div className="text-muted-foreground">{credits} credits</div>
  </CardContent>
  <CardFooter>
    <Button variant={isPopular ? "default" : "outline"}>Buy Now</Button>
  </CardFooter>
</Card>
```

**Credits page (credits/page.tsx):**
1. Read current structure
2. Update page header styling with text-foreground
3. Grid of credit cards: Consistent gap-6 spacing
4. Add descriptive text with text-muted-foreground
  </action>
  <verify>npm run dev - navigate to /dashboard/credits, cards display with premium styling, popular pack highlighted</verify>
  <done>Credit pack cards use Card, Badge, Button - popular pack has gradient highlight and enhanced styling</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Agent creation form uses shadcn/ui components
- [ ] Agent edit form uses shadcn/ui components
- [ ] Settings page has glass-card sections
- [ ] Transaction history styled with badges and typography
- [ ] Credit pack cards have premium styling with popular highlight
- [ ] All forms work correctly (submit, validation)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Forms feel consistent with overall dashboard styling
- Credit cards draw attention to popular pack
- No functionality regressions in forms
</success_criteria>

<output>
After completion, create `.planning/phases/12-dashboard-redesign/12-03-SUMMARY.md`
</output>
