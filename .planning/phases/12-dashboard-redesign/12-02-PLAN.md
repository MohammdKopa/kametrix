---
phase: 12-dashboard-redesign
plan: 02
type: execute
---

<objective>
Redesign stats cards, agent cards, and recent activity components with glassmorphism and gradient accents.

Purpose: Transform the main dashboard content components to match the premium dark theme.
Output: Polished card components with glassmorphic styling, gradient accents, and subtle glow effects.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-dashboard-redesign/12-CONTEXT.md
@.planning/phases/12-dashboard-redesign/12-01-SUMMARY.md

**Key files:**
@src/components/dashboard/stats-card.tsx
@src/components/dashboard/agent-card.tsx
@src/components/dashboard/recent-activity.tsx
@src/components/ui/card.tsx
@src/components/ui/badge.tsx

**From 12-CONTEXT.md:**
- Glassmorphic cards with blur effects and subtle borders (signature visual element)
- Purple/pink gradient accents
- Hover glow effects for interactivity
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Badge component and rebuild stats-card with shadcn/ui Card</name>
  <files>src/components/ui/badge.tsx, src/components/dashboard/stats-card.tsx</files>
  <action>
**First, add Badge component (if not exists):**
Run: npx shadcn@latest add badge

**Then rebuild stats-card.tsx:**
1. Import Card, CardContent from "@/components/ui/card"
2. Apply glass-card class to Card for glassmorphic effect
3. Icon container: Use gradient background (from-primary/20 to-accent/20)
4. Value text: Use text-foreground for main value
5. Warning state:
   - Border: border-amber-500/50
   - Icon bg: bg-amber-500/20
   - Add subtle amber glow on hover
6. Hover effect: Add shadow with accent glow (shadow-[0_0_30px_oklch(0.55_0.25_300/0.15)])
7. Keep existing props interface (title, value, subtitle, warning, icon)

Structure:
```
<Card className="glass-card hover:shadow-[0_0_30px_...]">
  <CardContent className="p-6">
    <div className="flex items-start justify-between">
      {/* Title, value, subtitle */}
      {/* Icon with gradient bg */}
    </div>
  </CardContent>
</Card>
```
  </action>
  <verify>npm run dev - check dashboard home, stats cards display with glass effect and hover glow</verify>
  <done>Stats cards use shadcn/ui Card, glassmorphic styling, gradient icon backgrounds</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild agent-card with shadcn/ui components</name>
  <files>src/components/dashboard/agent-card.tsx</files>
  <action>
Rebuild agent-card using shadcn/ui components:

1. Import Card, CardHeader, CardContent, CardFooter from "@/components/ui/card"
2. Import Button from "@/components/ui/button"
3. Import Badge from "@/components/ui/badge"
4. Import Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter from "@/components/ui/dialog"

**Card structure:**
- Apply glass-card class for glassmorphic effect
- Hover: shadow glow effect

**Status badge:**
- Use Badge component with variant based on isActive
- Active: variant="default" with green styling via className
- Inactive: variant="secondary"
- Add status dot inside badge

**Phone number display:**
- Use text-primary for the phone number (accent color)
- Copy button: Button variant="ghost" size="icon"

**Action buttons:**
- Toggle switch: Keep custom implementation (no shadcn switch needed)
- Edit button: Button variant="ghost" size="icon"
- Delete button: Button variant="ghost" size="icon" with hover:text-destructive

**Delete confirmation modal:**
- Replace custom modal with Dialog component
- DialogContent with glass effect (add glass class)
- DialogHeader with DialogTitle, DialogDescription
- DialogFooter with Cancel (variant="outline") and Delete (variant="destructive") buttons
  </action>
  <verify>npm run dev - check agents page, verify card styling, toggle works, delete dialog opens</verify>
  <done>Agent cards use shadcn/ui Card, Badge, Button, Dialog - all interactions work</done>
</task>

<task type="auto">
  <name>Task 3: Redesign recent-activity component with premium styling</name>
  <files>src/components/dashboard/recent-activity.tsx</files>
  <action>
First read the current recent-activity.tsx to understand its structure, then redesign:

1. Wrap in Card component with glass-card class
2. Add CardHeader with title "Recent Activity"
3. Use CardContent for activity list

**Activity items:**
- Use subtle separator between items (Separator component)
- Status indicators: Color-coded dots with glow effect
  - Success (completed): bg-green-500 with shadow-[0_0_8px_theme(colors.green.500/0.5)]
  - Warning (in-progress): bg-amber-500 with amber glow
  - Error (failed): bg-destructive with red glow
- Activity text: text-foreground for main, text-muted-foreground for secondary
- Timestamp: text-muted-foreground text-sm

**Empty state:**
- Keep icon + message pattern
- Use text-muted-foreground for message
- Add subtle gradient background or border

**Hover effect on items:**
- Subtle bg change: hover:bg-muted/50
- Slight scale or shadow effect
  </action>
  <verify>npm run dev - check dashboard home, recent activity displays with proper styling, empty state works</verify>
  <done>Recent activity uses Card, status indicators have glow effects, hover states work</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Stats cards have glassmorphic styling with hover glow
- [ ] Agent cards use shadcn/ui Card, Badge, Button, Dialog
- [ ] Agent toggle and delete work correctly
- [ ] Recent activity has premium styling with status indicator glows
- [ ] All components support both light and dark modes
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Card components feel cohesive with the header redesign from 12-01
- Glassmorphism is the signature visual element across all cards
- Gradient accents and glow effects create premium feel
</success_criteria>

<output>
After completion, create `.planning/phases/12-dashboard-redesign/12-02-SUMMARY.md`
</output>
