---
phase: 16-auth-ui-polish
plan: 1
type: execute
---

<objective>
Update login and signup pages to match the premium dark theme of the website.

Purpose: Create a cohesive brand experience from landing page through authentication to dashboard.
Output: Login and signup pages with glassmorphic styling, atmospheric glows, gradient accents, and motion animations matching the marketing site.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md

**Reference files for theme patterns:**
@src/app/(marketing)/layout.tsx - Dark background, grid pattern, gradient overlays
@src/components/marketing/Hero.tsx - Atmospheric glows, floating orbs, glassmorphic elements, motion animations

**Files to modify:**
@src/app/(auth)/layout.tsx - Add dark theme styling
@src/app/(auth)/login/page.tsx - Redesign with glassmorphic styling
@src/app/(auth)/signup/page.tsx - Redesign with glassmorphic styling

**shadcn/ui components to use:**
@src/components/ui/button.tsx
@src/components/ui/input.tsx
@src/components/ui/label.tsx
@src/components/ui/card.tsx

**Prior decisions affecting this phase:**
- OKLCH colors in Tailwind v4 (from Phase 11)
- motion/react for animations (from Phase 13)
- defaultTheme="dark" (from Phase 11)

**Theme specifications from marketing:**
- Background: `#06040d`
- Grid pattern: `rgba(255,255,255,0.1)` lines, 60px spacing, 3% opacity
- Gradient overlay: `from-purple-900/20 via-[#06040d] to-[#06040d]`
- Glassmorphic: `rgba(255,255,255,0.03)` bg, `blur(12px)`, `border-white/10`
- Gradient accents: purple-400 to pink-400
- Text: white/gray-200/gray-400/gray-500
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update auth layout with dark theme</name>
  <files>src/app/(auth)/layout.tsx</files>
  <action>
Transform the auth layout to match the marketing site's dark theme:

1. Add dark background `bg-[#06040d]` and `text-white`
2. Add subtle grid pattern overlay (60px squares, 3% opacity)
3. Add gradient background overlay from purple-900/20
4. Add atmospheric glow elements:
   - Primary purple glow (centered, 600x400px, blur-80)
   - Secondary magenta glow (offset, smaller)
5. Keep the centered card container layout
6. Use relative/absolute positioning for overlays with pointer-events-none
  </action>
  <verify>npm run build succeeds, auth pages render with dark background</verify>
  <done>Auth layout has dark theme with grid pattern and atmospheric glows</done>
</task>

<task type="auto">
  <name>Task 2: Redesign login page with glassmorphic styling</name>
  <files>src/app/(auth)/login/page.tsx</files>
  <action>
Redesign login page to match premium dark theme:

1. Remove bg-gray-50 background (now handled by layout)
2. Add 'use client' and import motion from 'motion/react'
3. Import shadcn/ui components: Button, Input, Label
4. Create glassmorphic card container:
   - `rgba(255,255,255,0.03)` background
   - `backdrop-blur-xl`
   - `border border-white/10`
   - Subtle shadow and rounded corners
5. Update logo/title:
   - Kametrix with purple gradient text (`from-purple-400 to-purple-600`)
   - Subtitle in gray-400
6. Style form inputs:
   - Use shadcn/ui Input with dark styling
   - Labels in gray-300
   - Add focus states with purple accent
7. Primary button styling:
   - Gradient background `from-purple-600 via-purple-500 to-pink-500`
   - Outer glow effect on hover
   - Loading state handling
8. Error state with red-500/10 background
9. Link styling with purple-400 accent
10. Add subtle entry animations with motion.div (fade-in, slide-up)
11. Keep all existing form logic and API calls intact
  </action>
  <verify>Visit /login - dark glassmorphic design, form submits correctly, error states work</verify>
  <done>Login page has premium dark theme with glassmorphic card, gradient button, animations</done>
</task>

<task type="auto">
  <name>Task 3: Redesign signup page with matching styling</name>
  <files>src/app/(auth)/signup/page.tsx</files>
  <action>
Apply same styling treatment as login page:

1. Remove bg-gray-50 background (handled by layout)
2. Add 'use client' and import motion from 'motion/react'
3. Import shadcn/ui components: Button, Input, Label
4. Create glassmorphic card container (same styling as login)
5. Update logo/title with gradient text
6. Style all form inputs (name, email, password) with dark theme
7. Password hint text in gray-500
8. Primary button with gradient and glow effects
9. Error state styling
10. Link styling with purple-400 accent
11. Add entry animations matching login page
12. Keep all existing form logic and API calls intact
13. Maintain the name field as optional with gray-500 indicator
  </action>
  <verify>Visit /signup - matches login styling, form submits correctly, all validations work</verify>
  <done>Signup page has matching premium dark theme, all form functionality preserved</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run lint` passes
- [ ] Login page at /login renders with dark glassmorphic design
- [ ] Signup page at /signup matches login styling
- [ ] Form submissions work (test with valid/invalid data)
- [ ] Error states display correctly
- [ ] Navigation between login/signup works
- [ ] No TypeScript errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Login and signup pages match the website's premium dark theme
- Glassmorphic cards with blur effects
- Gradient accents (purple to pink)
- Atmospheric glows in background
- Entry animations with motion/react
- All existing form functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/16-auth-ui-polish/16-01-SUMMARY.md`:

# Phase 16 Plan 1: Auth UI Polish Summary

**[Substantive one-liner]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase complete, ready for Phase 17: Admin Price Control
</output>
