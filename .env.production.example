# =============================================================================
# Kametrix Production Environment Variables
# =============================================================================
# Copy this file to .env on your server and fill in all values.
# All variables without defaults are REQUIRED.
# =============================================================================

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------

# Your domain name (used for OAuth redirects and SSL)
DOMAIN=app.yourdomain.com

# Full public URL of your application (must match DOMAIN with https://)
NEXT_PUBLIC_APP_URL=https://app.yourdomain.com

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# These configure the PostgreSQL container. DATABASE_URL is auto-generated
# in docker-compose.prod.yml from these values.

# PostgreSQL username (default: kametrix)
POSTGRES_USER=kametrix

# PostgreSQL password - CHANGE THIS to a strong, random password
# Generate with: openssl rand -base64 32
POSTGRES_PASSWORD=CHANGE_THIS_TO_A_STRONG_PASSWORD

# PostgreSQL database name (default: kametrix)
POSTGRES_DB=kametrix

# -----------------------------------------------------------------------------
# Vapi Configuration
# -----------------------------------------------------------------------------
# Get your API key from https://vapi.ai/dashboard

VAPI_API_KEY=your_vapi_api_key

# -----------------------------------------------------------------------------
# Google OAuth Configuration
# -----------------------------------------------------------------------------
# Set up OAuth 2.0 credentials in Google Cloud Console:
# 1. Go to https://console.cloud.google.com/apis/credentials
# 2. Create OAuth 2.0 Client ID (Web application)
# 3. Add authorized redirect URI: https://your-domain.com/api/auth/google/callback
# 4. Enable Google Calendar API in your project

GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# Encryption key for storing Google refresh tokens
# Generate with: openssl rand -hex 32
GOOGLE_ENCRYPTION_KEY=your_64_character_hex_key

# -----------------------------------------------------------------------------
# Stripe Configuration
# -----------------------------------------------------------------------------
# Get your keys from https://dashboard.stripe.com/apikeys

# Secret key (starts with sk_live_ or sk_test_)
STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key

# Webhook signing secret (starts with whsec_)
# Create webhook at https://dashboard.stripe.com/webhooks
# Point to: https://your-domain.com/api/webhooks/stripe
# Events needed: checkout.session.completed
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# -----------------------------------------------------------------------------
# SMTP Configuration (Email Notifications)
# -----------------------------------------------------------------------------
# Configure your SMTP server for sending emails (welcome emails, low credit alerts)

# SMTP server hostname
SMTP_HOST=smtp.yourdomain.com

# SMTP port (typically 587 for TLS, 465 for SSL, 25 for plain)
SMTP_PORT=587

# SMTP authentication credentials
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

# From address for outgoing emails
SMTP_FROM=Kametrix <noreply@yourdomain.com>

# -----------------------------------------------------------------------------
# OpenRouter Configuration (Optional - AI Text Generation)
# -----------------------------------------------------------------------------
# Get your API key from https://openrouter.ai/keys
# Used for generating agent descriptions and other AI features

OPENROUTER_API_KEY=your_openrouter_api_key
